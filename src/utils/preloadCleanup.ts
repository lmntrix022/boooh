/**
 * @deprecated Ce fichier est déprécié. Utilisez preloadCleanupService à la place.
 * Migration: import { preloadCleanupService, PreloadCleanup } from '@/services/cache/preloadCleanupService';
 */
export {
  preloadCleanupService,
  PreloadCleanup
} from '../services/cache/preloadCleanupService';
