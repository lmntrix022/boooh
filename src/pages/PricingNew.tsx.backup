/**
 * PricingNew - Page tarifaire complète avec les 4 nouveaux plans
 * Design minimaliste gray épuré
 */

import React, { useState } from 'react';
import { motion } from 'framer-motion';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Card } from '@/components/ui/card';
import { Check, X, ArrowRight } from 'lucide-react';
import PricingNewPlans from '@/components/pricing/PricingNewPlans';
import ROICalculatorInteractive from '@/components/pricing/ROICalculatorInteractive';
import OperePackagesSection from '@/components/pricing/OperePackagesSection';
import { useNavigate } from 'react-router-dom';
import { cn } from '@/lib/utils';

interface FeatureComparison {
  category: string;
  features: {
    name: string;
    essentiel: boolean | string;
    connexions: boolean | string;
    commerce: boolean | string;
    opere: boolean | string;
  }[];
}

const FEATURE_COMPARISON: FeatureComparison[] = [
  {
    category: 'Identité & Visibilité',
    features: [
      { name: 'Carte de visite digitale', essentiel: true, connexions: true, commerce: true, opere: true },
      { name: 'Portfolio produits/services', essentiel: '10 items', connexions: '50 items', commerce: 'Illimité', opere: 'Illimité' },
      { name: 'Page entreprise personnalisée', essentiel: true, connexions: true, commerce: true, opere: 'Premium' },
      { name: 'SEO optimisé', essentiel: false, connexions: true, commerce: true, opere: 'Avancé' },
    ],
  },
  {
    category: 'Relation Client (CRM)',
    features: [
      { name: 'Gestion contacts', essentiel: false, connexions: true, commerce: true, opere: true },
      { name: 'Prise de rendez-vous', essentiel: false, connexions: true, commerce: true, opere: true },
      { name: 'Historique interactions', essentiel: false, connexions: true, commerce: true, opere: true },
      { name: 'Segmentation avancée', essentiel: false, connexions: false, commerce: true, opere: true },
    ],
  },
  {
    category: 'Commerce & Paiements',
    features: [
      { name: 'Ventes en ligne', essentiel: false, connexions: false, commerce: true, opere: true },
      { name: 'Paiements intégrés (BoohPay)', essentiel: false, connexions: false, commerce: true, opere: true },
      { name: 'Gestion stock', essentiel: false, connexions: false, commerce: true, opere: true },
      { name: 'Facturation automatique', essentiel: false, connexions: false, commerce: true, opere: true },
    ],
  },
  {
    category: 'Marketing & Croissance',
    features: [
      { name: 'Analytics de base', essentiel: true, connexions: true, commerce: true, opere: true },
      { name: 'Campagnes marketing', essentiel: false, connexions: false, commerce: false, opere: true },
      { name: 'Stratégie de croissance', essentiel: false, connexions: false, commerce: false, opere: true },
      { name: 'Consultant dédié', essentiel: false, connexions: false, commerce: false, opere: true },
    ],
  },
];

const PricingNew: React.FC = () => {
  const navigate = useNavigate();
  const [showComparison, setShowComparison] = useState(false);

  return (
    <div className="min-h-screen bg-gradient-to-b from-white via-gray-50 to-white">
        {/* Hero Section */}
        <section className="relative py-16 md:py-24 overflow-hidden">
          <div className="absolute inset-0 bg-gradient-to-br from-transparent via-gray-50/20 to-transparent" />
          
          <div className="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <motion.div
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
            >
              <Badge className="mb-6 bg-gray-900 text-white border-0 rounded-full px-6 py-2 text-sm font-medium">
                Offres 2026 • 4 plans modulaires
              </Badge>
              <h1 className="text-5xl md:text-7xl font-extralight text-gray-900 mb-6 tracking-tight">
                Tarifaire simple,{' '}
                <span className="bg-gradient-to-r from-gray-600 to-gray-900 bg-clip-text text-transparent">
                  transparent
                </span>
              </h1>
              <p className="text-xl md:text-2xl text-gray-600 max-w-3xl mx-auto font-light leading-relaxed">
                Choisissez l'offre qui correspond à vos ambitions. 
                De l'essai gratuit au partenariat premium, zéro surprise.
              </p>
            </motion.div>
          </div>
        </section>

        {/* Plans Section */}
        <section className="py-12">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <PricingNewPlans />
          </div>
        </section>

        {/* ROI Calculator */}
        <section className="py-16 bg-white">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <ROICalculatorInteractive />
          </div>
        </section>

        {/* Opéré Packages */}
        <section className="py-16 bg-gray-50">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <OperePackagesSection />
          </div>
        </section>

        {/* Feature Comparison Table */}
        <section className="py-16 bg-white">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div className="text-center mb-12">
              <h2 className="text-3xl md:text-4xl font-light text-gray-900 mb-4">
                Comparaison détaillée
              </h2>
              <p className="text-lg text-gray-600 font-light">
                Toutes les fonctionnalités en un coup d'œil
              </p>
            </div>

            <div className="overflow-x-auto">
              <table className="w-full border-collapse bg-white rounded-2xl shadow-sm overflow-hidden">
                <thead>
                  <tr className="border-b-2 border-gray-200">
                    <th className="text-left p-4 text-sm font-semibold text-gray-900 bg-gray-50 min-w-[200px]">
                      Fonctionnalités
                    </th>
                    <th className="text-center p-4 text-sm font-semibold text-gray-900 bg-gray-50">
                      Essentiel
                    </th>
                    <th className="text-center p-4 text-sm font-semibold text-gray-900 bg-gray-100">
                      Connexions
                    </th>
                    <th className="text-center p-4 text-sm font-semibold text-gray-900 bg-gray-50">
                      Commerce
                    </th>
                    <th className="text-center p-4 text-sm font-semibold text-gray-900 bg-gray-100">
                      Opéré
                    </th>
                  </tr>
                </thead>
                <tbody>
                  {FEATURE_COMPARISON.map((category, catIdx) => (
                    <React.Fragment key={catIdx}>
                      <tr className="bg-gray-50">
                        <td colSpan={5} className="p-3 font-medium text-sm text-gray-900">
                          {category.category}
                        </td>
                      </tr>
                      {category.features.map((feature, featIdx) => (
                        <tr key={featIdx} className="border-b border-gray-100 hover:bg-gray-50/20 transition-colors">
                          <td className="p-4 text-sm text-gray-700">{feature.name}</td>
                          {(['essentiel', 'connexions', 'commerce', 'opere'] as const).map((plan) => (
                            <td 
                              key={plan}
                              className={`text-center p-4 ${plan === 'connexions' || plan === 'opere' ? 'bg-gray-50/30' : ''}`}
                            >
                              {typeof feature[plan] === 'boolean' ? (
                                feature[plan] ? (
                                  <Check className="h-5 w-5 text-gray-700 mx-auto" />
                                ) : (
                                  <X className="h-5 w-5 text-gray-300 mx-auto" />
                                )
                              ) : (
                                <span className="text-sm text-gray-700 font-medium">
                                  {feature[plan]}
                                </span>
                              )}
                            </td>
                          ))}
                        </tr>
                      ))}
                    </React.Fragment>
                  ))}
                </tbody>
              </table>
            </div>
          </div>
        </section>

        {/* FAQ Section */}
        <section className="py-16 bg-gray-50">
          <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
            <div className="text-center mb-12">
              <h2 className="text-3xl md:text-4xl font-light text-gray-900 mb-4">
                Questions fréquentes
              </h2>
            </div>

            <div className="space-y-4">
              {[
                {
                  q: 'Puis-je changer de plan à tout moment ?',
                  a: 'Oui ! Vous pouvez upgrader ou downgrader à tout moment. Les changements sont effectifs immédiatement.',
                },
                {
                  q: 'Comment fonctionne la commission sur Commerce et Opéré ?',
                  a: 'La commission est prélevée uniquement sur les ventes réalisées via BoohPay. Pas de ventes = 0€ de coût.',
                },
                {
                  q: 'Que comprend le setup Opéré ?',
                  a: 'Le setup varie de 50K à 500K selon le package. Il inclut configuration, marketing, formation et accompagnement dédié.',
                },
                {
                  q: 'Y a-t-il des frais cachés ?',
                  a: 'Non. Tous nos tarifs sont transparents. Pas de frais de mise en service (sauf setup Opéré), pas de frais de transaction cachés.',
                },
              ].map((faq, idx) => (
                <Card key={idx} className="p-6 bg-white border border-gray-200 hover:shadow-md transition-shadow">
                  <h3 className="font-medium text-gray-900 mb-2">{faq.q}</h3>
                  <p className="text-sm text-gray-700 leading-relaxed">{faq.a}</p>
                </Card>
              ))}
            </div>
          </div>
        </section>

        {/* CTA Final */}
        <section
          className="bg-gray-900 rounded-3xl mx-4 md:mx-8 my-16 p-12 md:p-16 text-center text-white"
        >
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
          >
            <h2 className="text-3xl md:text-4xl font-light mb-6">
              Prêt à démarrer votre croissance ?
            </h2>
            <p className="text-lg text-gray-300 mb-8 max-w-2xl mx-auto font-light">
              Rejoignez des centaines d'entreprises qui font confiance à BÖÖH pour leur transformation digitale
            </p>
            <div className="flex flex-col sm:flex-row gap-4 justify-center items-center">
              <Button
                onClick={() => navigate('/auth')}
                size="lg"
                className="bg-white text-gray-900 hover:bg-gray-100 rounded-xl px-8 py-6 text-lg font-medium"
              >
                Commencer gratuitement
                <ArrowRight className="h-5 w-5 ml-2" />
              </Button>
              <Button
                onClick={() => navigate('/contact')}
                size="lg"
                variant="outline"
                className="bg-white text-gray-900 hover:bg-gray-100 rounded-xl px-8 py-6 text-lg font-medium"
              >
                Parler à un expert
              </Button>
            </div>
          </motion.div>
        </section>
      </div>
    </>
  );
};

export default PricingNew;
